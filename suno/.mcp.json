{
  "mcpServers": {
    "suno": {
      "type": "stdio",
      "command": "python3",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/server.py"],
      "note": "Local server with no pip dependencies. NO tool makes requests to Suno: only session diagnostics (reads the JWT from the cookies JSON) and verification of already-downloaded audio (via ffmpeg). The actual operation goes through the browser — see the suno-browser skill."
    }
  }
}
