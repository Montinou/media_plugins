{
  "mcpServers": {
    "example": {
      "type": "stdio",
      "command": "python3",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/server.py"],
      "note": "Local server with no pip dependencies: JSON-RPC over stdio using the stdlib only. Rename the key and SERVER_NAME when you adapt it."
    }
  }
}
