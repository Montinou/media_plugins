{
  "mcpServers": {
    "google-flow": {
      "type": "stdio",
      "command": "python3",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp/server.py"
      ],
      "note": "Local server with no pip dependencies: JSON-RPC over stdio with the stdlib. Needs playwright, requests, and pillow in the system python3, Google Chrome installed, and the labs.google cookies at ~/.config/google-flow/labs.google.cookies.json (or FLOW_COOKIES). The core doesn't depend on any project: project-specific stuff goes in packs/."
    }
  }
}
